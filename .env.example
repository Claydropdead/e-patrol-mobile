# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Backend API Configuration (use your computer's IP address, not localhost)
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.XXX:3000

# Note: 
# - Copy this file to .env and fill in your actual values
# - Never commit .env file to git (it's in .gitignore)
# - Use EXPO_PUBLIC_ prefix for variables that need to be available in the app
# - Replace localhost with your computer's IP address for mobile testing
# - Run "ipconfig" (Windows) or "ifconfig" (Mac/Linux) to find your IP address
